# Copy this file to .env and fill in your local PostgreSQL credentials.
# .env is gitignored and should never be committed.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=engineering_delivery
DB_USER=postgres
DB_PASSWORD=
